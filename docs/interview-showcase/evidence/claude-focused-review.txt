**No high or medium regressions found in the three repairs.**

Static review of the five requested files only. No commands were run, so the test suite and type-check results are not verified here.

**Repair 1: grouped reference text (`lib/ask-citations.ts:321-342`)**

  • **Holds.** Lists, ranges, and `ref`/`source:` groups now expand into single markers before validation. A range like `[v1-v3]` expands to three refs, and any missing one is flagged by the marker pass at `ask-citations.ts:343-349`. Numeric-only ranges like `[1-3]` fail the letter-prefix range regex and land in unknown refs. Covered by `tests/ask-security.test.ts:234-240` and `tests/ask-citations.test.ts:184-191`.

  • **Low, by design.** Bracket text that does not begin with a ref-like token, such as `[current member roster]` or `[ref: v1]`, is left as literal text rather than rejected. It is never counted as a citation, so it is not a grounding bypass.

**Repair 2: unrecognized state questions (`lib/ask-tools.ts:260-262`)**

  • **Holds.** No matched route now returns the full scoped tool set. The test at `ask-security.test.ts:226-232` confirms biography and bills are exposed for both state and national scope.

  • **Low.** The `!hasRetrieval ||` clause at `ask-tools.ts:265` is now dead code, since the early return above guarantees it is false. Harmless.

**Repair 3: spending field (`lib/ask-tools.ts:702`, `scripts/eval-ask.ts:147-160`)**

  • **Holds.** `total_disbursements` is emitted per cycle, the finance note at line 692 distinguishes receipts from disbursements, and the router at line 251 matches spend/spent/spending. The eval question's "election cycle" is normalized at line 246, so it does not also trigger the race route.

  • **Low.** The spending eval hardcodes `Y000064` at `eval-ask.ts:154`, while sibling cases resolve by name and `in_office`. It throws instead of skipping when no ground truth exists, which surfaces as ERROR and fails the target. That is stricter, not wrong.

**Static-review limits**

  • I did not open `lib/queries.ts`, so I did not confirm `getMemberFinance` returns a `totalDisbursements` property. A mismatch would fail `next build`, which is your existing gate.

  • Engine-side handling of `unknownRefs` lives in `lib/ask-engine.ts`, outside this review. The tests assert it throws "did not retrieve", which is the expected behavior.
