**Review done: no high findings, three medium, a few low. Fixed items hold up; /about claims match the code.**

Verified and not repeated here: the citation gate order in `finalizeAskAnswer`, fork/adopt in `prepareToolPayload`, cache invalidation (both crawlers log to sync_log, review scripts set `reviewed_at`, columns exist at `scripts/schema.sql:743,772,840`), the quote-presence check behind the /about claim (`lib/elections/campaign-research.ts:161,190`, `lib/biography-research.ts:75`), and eval failure behavior (an ERROR case counts as a fail and sets exit code 1 at `scripts/eval-ask.ts:496-507,581`).

**Medium findings**

  • **Unrouted state-scope questions on Anthropic get only the roster tool.** `lib/ask-tools.ts:261-266` adds get_delegation when no topic route matches, but the member-scope fallback at `lib/ask-tools.ts:274-285` has no state-scope equivalent. The engine then forces that single tool on the first turn (`lib/ask-engine.ts:377-379`). Roster records carry `d` refs (`lib/ask-citations.ts:234-247`), so a memory-derived claim plus `[d1]` passes the gate as "answered". Example: "What has Todd Young said about Ukraine?" matches no route. The OpenAI path exposes every tool, so this is a provider asymmetry, not a disclosed limitation.

  • **Spending questions route to a payload with no spending field.** `lib/ask-tools.ts:251` routes spent, spend, spending, expenditure to get_member_finance, but the by_cycle mapping at `lib/ask-tools.ts:708-714` emits receipts only. The column exists (`scripts/schema.sql:165`) and `getMemberFinance` already selects it (`lib/queries.ts:406-419`). `tests/ask-security.test.ts:206` locks the route in. A receipts-as-spending answer carries a valid `[f1]` and nothing catches it. Fix is one field plus a note.

  • **Citation-looking text in non-canonical form is neither validated nor removed.** `MARKER_RE` at `lib/ask-citations.ts:30` matches only a single `[x1]`. Forms like `[v1, v2]`, `[f7; f8]`, or `[ref v9]` are left in the answer verbatim, and the client renders them as plain text (`components/ask-client.tsx:212`). An answer with one real `[v1]` and a fabricated `[f7, f8]` passes `finalizeAskAnswer` and shows the fake markers to the reader. Reject or strip any remaining bracket group of ref-shaped tokens after resolution.

**Low findings**

  • **Health "eligible" counters overstate published quotes.** `lib/biography-queries.ts:104-114` counts raw rows while the published query dedupes (`lib/biography-queries.ts:62-72,84-93`). `lib/elections/queries.ts:1079-1087` counts service rows without the office filters applied at `lib/elections/queries.ts:597-606`. Member crawl errors count all sites (`lib/biography-queries.ts:116`) while candidate crawl errors count verified only (`lib/elections/queries.ts:1090`).

  • **Anthropic zero-tool-use turn skips fallback.** `lib/ask-engine.ts:407-409` throws a 502 AskError when no tool block is returned, such as a max_tokens stop. The OpenAI loop treats the equivalent `incomplete_details` as provider-unavailable and falls back (`lib/ask-engine.ts:504-506`).

  • **Misleading terminal error.** If the deadline passes after the primary fails, `lib/ask-engine.ts:588` breaks and `lib/ask-engine.ts:639-644` reports both providers unavailable though the second was never tried.

  • **Same-turn submit_answer plus retrieval drops the retrievals.** Both loops (`lib/ask-engine.ts:410-417`, `525-529`) return on submit_answer before executing sibling calls. Refs from those calls then read as unknown and the run fails closed. Safe, but a wasted paid run.

No plan file was written. The session only exposed read tools, and the deliverable is this assessment.
